<%= form_with(model: profile, remote: true) do |form| %>
  <% if profile.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(profile.errors.count, "error") %> prohibited this profile from being saved:</h2>

      <ul>
      <% profile.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field form-group field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id, id: :profile_user_id, class: 'form-control' %>
  </div>

  <div class="field form-group field">
    <%= form.label :first_name %>
    <%= form.text_field :first_name, id: :profile_first_name, class: 'form-control' %>
  </div>

  <div class="field form-group field">
    <%= form.label :last_name %>
    <%= form.text_field :last_name, id: :profile_last_name, class: 'form-control' %>
  </div>

  <div class="field form-group field">
    <%= form.label :phone %>
    <%= form.text_field :phone, id: :profile_phone, class: 'form-control' %>
  </div>

  <div class="actions">
    <%= form.submit class: 'btn btn-success pull-right' %>
  </div>
<% end %>
