
    <%= render partial: 'home/main/adam_test_section' %>

    <%= render partial: 'home/main/section_portfolio' %>
    <%= render partial: 'home/main/section_about' %>

    <%= render partial: 'home/main/section_team' %>
    <%= render partial: 'home/main/section_clients' %>
    <%= render partial: 'home/main/footer' %>

 
    <!-- Portfolio Modal -->
  <% @spaces.each do |space| %>

    <div class="portfolio-modal modal fade" id="portfolioModal<%= space.id %>" tabindex="-1" role="dialog" aria-hidden="true">
      <div class="modal-dialog">
        <div class="modal-content">
          <div class="close-modal" data-dismiss="modal">
            <div class="lr">
              <div class="rl"></div>
            </div>
          </div>
          <div class="container">
            <div class="row">
              <div class="col-lg-8 mx-auto">
                <div class="modal-body">
                  <!-- Project Details Go Here -->
                  <h2 class="text-uppercase"> <%= space.name %></h2>
                  <p class="item-intro text-muted"> <%= space.description %></p>
                  <!-- <img class="img-fluid d-block mx-auto" src="img/portfolio/01-full.jpg" alt=""> -->
                  <p><%= space.website %></p>
                  <ul class="list-inline">
                  <% space.amenities.each do |amenity| %>
                    <li><%= amenity.name %></li>
                  <% end %>
                  </ul>
                  <%= link_to "More info", space, class: "cta btn btn-primary btn-xl text-uppercase js-scroll-trigger" %>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>

<% end %>