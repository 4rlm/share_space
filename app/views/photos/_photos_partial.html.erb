<%#= render partial: 'upload_image_modal' %>

<% if @photos %>
  <% @photos.each do |photo| %>
    <div class="col-md-2">
      <%= render 'photos/photo_partial', photo: photo %>
    </div>
  <% end %>
<% end %>
